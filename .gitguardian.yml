version: 2

# DDL scripts and data files use placeholder values that are not real secrets.
# Users must substitute real values before deploying to any environment.
paths-ignore:
  - "sql/**"
  - "**/*.sql"
  - "**/*.csv"
  - "**/*.xlsx"
  - "**/*.docx"
  - "**/*.pdf"
